<link href="/assets/custom.css" media="screen" rel="stylesheet">
<link href="/assets/bootstrap.css" media="screen" rel="stylesheet">
<%= link_to 'My Projects', controller: 'projects' %>

<h1> Edit comment</h1>

 
<%= form_for :comment, url: task_comment_path(@comment), method: :patch do |f| %>
 
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@comment.errors.count, "error") %> prohibited
        this comment from being saved:
      </h2>
      <ul>
        <% @comment.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= f.label :body %><br>
    <%= f.text_field :body %>
  </p>
  
 
  <p>
    <%= f.submit %>
  </p>
 
<% end %>
